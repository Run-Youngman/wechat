<view>
  <view class="index-hd">
    <image class="index-logo" src='../../image/logo.png'></image>
    <view class="index-desc">以下将展示小程序官方组件能力，组件样式仅供参考，开发者可根据自身需求自定义组件样式，具体属性参数详见小程序开发文档。</view>
  </view>
  <!-- 最外层的大框 -->
  <view>
    <view>
      <block wx:for="{{list}}" wx:key="{{item.id}}">
        <view>{{item.name}}</view>
        <view wx:for="{{item.pages}}" wx:for-item="page" wx:key="*item">
          <navigator url="page/{{page}}/{{page}}" class="navigator">
            <view class="navigator-text">{{page}}</view>
            <view class="navigator-arrow"></view>
          </navigator>
        </view>
      </block>
    </view>
    
  </view>
</view>